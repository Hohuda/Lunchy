.row.justify-content-center
  .col-6
    %h2.text-center Delete Category
    = simple_form_for(:noname, url: categories_path, html: {method: :delete}) do |f|
      = f.error_notification
      .form-inputs
        = f.association :categories, autofocus: true
      .form-actions.text-center
        = f.button :submit, "Delete", class: 'btn btn-danger w-25'