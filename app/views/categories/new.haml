.row.justify-content-center
  .col-6
    %h2.text-center Add New Category
    = simple_form_for(@category, url: categories_path, html: {method: :post}) do |f|
      = f.error_notification
      .form-inputs
        = f.input :name, label: "Name", required: true, autofocus: true
      .form-actions.text-center
        = f.button :submit, "Add", class: 'btn btn-primary w-25'