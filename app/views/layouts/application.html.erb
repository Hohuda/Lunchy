<!DOCTYPE html>
<html>
  <head>
    <title>LunchyApp</title>
    <%= render 'layouts/head' %>
  </head>
 
  <body>
    <header>
      <%= render 'layouts/header' %>
    </header>
    
    <div class="container jumbotron">
      <%= yield %>
    </div>

    <footer class="container">
      <%= link_to 'Contact', 'contact', class: 'text-secondary' %>
    </footer>
    
    <div class="container">
      <%= debug(params) if Rails.env.development? %>
    </div>
    
  </body>

  </html>