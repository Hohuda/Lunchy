<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
  </head>
  <body>
      <header class="navbar navbar-expand-sm bg-dark">
        <%= render 'layouts/header' %>
      </header>
      <div class="container">
          <%= yield %>
      </div>
    </body>
  </html>