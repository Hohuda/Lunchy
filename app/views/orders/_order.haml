%li.list-group-item
  .row
    .col
      %h5 Order ##{order.id}
    .col-auto
      = link_to 'Details', order_path(order), class: 'btn btn-secondary'
  .row
    .col
      = link_to order.user.name, user_path(order.user)
    .col-auto
      = time_ago_in_words(order.updated_at) 
      ago