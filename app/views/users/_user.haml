%li
  .row.list-partial
    .col-sm-auto
      = avatar_for(user, version: :xsmall)
    .col-sm
      = link_to user.name, user
    .col-sm-auto
      = link_to 'Orders', orders_orders_of_user_path(user_id: user.id)
  %br